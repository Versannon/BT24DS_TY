# Topic 1: Introduction to Supervised Learning

## Definition
Supervised learning is a paradigm where a model learns a mapping from inputs $X$ to targets $y$ using labeled training data.

## Core Tasks
- **Regression**: Target is continuous (e.g. stock price, temperature).
- **Classification**: Target is discrete (e.g. class labels like dog/cat, fraud/no-fraud).
