# Topic 1: Point Processing and Histogram Equalization

- **Point Processing**: Operates on single pixels. E.g. Contrast stretching, Log transformation ($s = c \log(1 + r)$), Gamma correction ($s = c r^\gamma$).
- **Histogram Equalization**: Map input CDF to uniform distribution to automatically enhance contrast.
